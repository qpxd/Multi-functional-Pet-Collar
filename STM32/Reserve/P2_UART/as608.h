#ifndef __AS608_H
#define __AS608_H

#define PS_Sta   PAin(6)//??????????????
#define CharBuffer1 0x01
#define CharBuffer2 0x02

extern u32 AS608Addr;//?????

typedef struct  
{
	u16 pageID;//???ID
	u16 mathscore;//???¡Â?
}SearchResult;

typedef struct
{
	u16 PS_max;//??????????
	u8  PS_level;//??????
	u32 PS_addr;
	u8  PS_size;//?????????§³
	u8  PS_N;//?????????N
}SysPara;

void PS_StaGPIO_Init(void);//?????PA6????????
	
u8 PS_GetImage(void); //?????? 
 
u8 PS_GenChar(u8 BufferID);//???????? 

u8 PS_Match(void);//???????????????? 

u8 PS_Search(u8 BufferID,u16 StartPage,u16 PageNum,SearchResult *p);//??????? 
 
u8 PS_RegModel(void);//???????????????‰Î 
 
u8 PS_StoreChar(u8 BufferID,u16 PageID);//??????? 

u8 PS_DeletChar(u16 PageID,u16 N);//?????? 

u8 PS_Empty(void);//??????? 

u8 PS_WriteReg(u8 RegNum,u8 DATA);//§Õ??????? 
 
u8 PS_ReadSysPara(SysPara *p); //???????????? 

u8 PS_SetAddr(u32 addr);  //????????? 

u8 PS_WriteNotepad(u8 NotePageNum,u8 *content);//§Õ???¡À? 

u8 PS_ReadNotepad(u8 NotePageNum,u8 *note);//?????? 

u8 PS_HighSpeedSearch(u8 BufferID,u16 StartPage,u16 PageNum,SearchResult *p);//???????? 
  
u8 PS_ValidTempleteNum(u16 *ValidN);//????§¹?????? 

u8 PS_HandShake(u32 *PS_Addr); //??AS608???????

const char *EnsureMessage(u8 ensure);//???????????????

void as608_init(void);
int press_FR(void);
void Add_FR(void);

#endif

